schemaVersion: 2.0.0

fileExistenceTests:
  - name: "Whisparr binary"
    path: "/app/Whisparr"
    shouldExist: true

  - name: "Config directory"
    path: "/config"
    shouldExist: true

metadataTest:
  workdir: "/config"
  volumes: ["/config"]
  entrypoint:
    ["/usr/bin/catatonit", "--", "/entrypoint.sh"]
